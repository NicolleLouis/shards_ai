# Expérience exp-00073

## Décision

rejected

## Hypothèse

Replacing gamma=0.995 with gamma=1.0 in a PPO continuation from active v002 will preserve the terminal win/loss signal and improve playing strength without regressing the required opponents.

## Évolution du catalogue d'idées

diff --git a/doc/Ideas.md b/doc/Ideas.md
index 6da964f..dc805a7 100644
--- a/doc/Ideas.md
+++ b/doc/Ideas.md
@@ -628,3 +628,27 @@ robuste contre Random, v002, v007 et v008, et dérive d'argmax v002 dans le budg
 rejet : échec de reproduction, régression Random persistante, gain limité à v007/v008, ou amélioration
 qui disparaît avec un panel indépendant. Le runtime, le débit et le nombre d'actions restent des
 métriques secondaires et ne peuvent pas compenser un échec de qualité.
+
+## Expérience exp-00073 — PPO — rejetée
+
+- [Terminé] Tester une continuation PPO depuis `configs/neural_profiles/v002.pt` en remplaçant
+  `gamma=0,995` par `gamma=1,0`, avec architecture, moteur, heuristiques et masque inchangés.
+- [Résultat] Le smoke test a écrit un checkpoint, mais la campagne prévue de 128 parties n'a
+  sauvegardé que le premier palier de 16 parties (2 885 transitions), puis s'est arrêtée
+  silencieusement, y compris lors de la reprise.
+- [Résultat] Sur 20 parties par adversaire, les deltas contre v002 sont Random `+10`, v007 `-5`,
+  v008 `0` et neural v002 `-10` points. Le gain Random isolé ne se généralise pas ; aucune
+  promotion n'est justifiée.
+- [Résultat] Le benchmark comparable passe de `26,2268 s` à `25,8814 s` (`-1,32 %`), avec
+  `6 668` contre `7 057` actions ; cette variation ne constitue pas un gain de qualité.
+- [Supprimé] Ne pas reprendre `gamma=1,0` seul ni considérer un palier PPO partiel comme une
+  campagne suffisante.
+
+## Nouvelles idées après exp-00073
+
+- [À privilégier] Instrumenter un rollout PPO unique avec résultats terminaux, variance des
+  avantages et norme des gradients avant toute campagne longue ; arrêter si le signal est quasi nul.
+- [À étudier] Comparer `gamma=0,995` et `gamma=1,0` sur un même budget réellement atteint, avec
+  reprise vérifiée après chaque palier et holdout séparé.
+- [À conserver] Exiger Random, v007, v008 et neural v002, ainsi qu'un runtime comparable ; le gain
+  contre Random seul ne suffit jamais.

## Provenance

```json
{
  "budget_seconds": 3600,
  "campaign_id": "campaign-20260806-152324-f0a6e2",
  "dataset": null,
  "dataset_records": null,
  "dataset_sha256": null,
  "experiment_family": "ppo",
  "experiment_kind": "quality",
  "novelty": "An isolated terminal-return horizon change correcting exp-00071's unchanged gamma, with unchanged engine, heuristics, information mask, model architecture and v002 initialization.",
  "parent_commit": "08486a567afdade2e268b2568fba08878103dec3",
  "parent_profile": "v002",
  "seed": 104,
  "teacher_profile": null
}
```

## Changements autorisés

```json
[
  "doc/Ideas.md",
  "result.json",
  "configs/neural_training_profiles/candidates/exp00073-ppo-gamma1.yaml",
  "doc/Experiments/exp-00073.md"
]
```

## Recette d'entraînement

```json
{
  "actual_training": {
    "games": 16,
    "resumed": true,
    "transitions": 2885,
    "updates": 1
  },
  "gae_lambda": 0.95,
  "games_per_update": 16,
  "gamma": 1.0,
  "initial_checkpoint": "configs/neural_profiles/v002.pt",
  "learning_rate": 0.0001,
  "minibatch_size": 256,
  "optimization_epochs": 4,
  "parent_profile": "v002",
  "reset_optimizer": true,
  "seed": 73073,
  "smoke_test": {
    "checkpoint_written": true,
    "games": 1,
    "transitions": 153
  },
  "torch_threads": 1,
  "total_games_requested": 128
}
```

## Commandes

```json
[
  "codex exec --sandbox workspace-write --ephemeral -",
  "poetry run pytest -q"
]
```

## Résultats offline

```json
{}
```

## Observations analytiques

```json
{}
```

## Limites de l'analyse

```json
{}
```

## Recommandations

```json
{}
```

## Résultats de screening

```json
{}
```

## Validation

```json
{
  "accepted": false,
  "protocol": "20 games per opponent, same seeds, candidate and active v002 reference; repository validation primitives",
  "reason": "v007 and neural:v002 regressions; Random improvement alone is insufficient",
  "reference_profile": "v002",
  "results": {
    "neural:v002": {
      "candidate_win_rate": 0.4,
      "delta_win_rate": -0.1,
      "games": 20,
      "reference_win_rate": 0.5
    },
    "random": {
      "candidate_win_rate": 1.0,
      "delta_win_rate": 0.1,
      "games": 20,
      "reference_win_rate": 0.9
    },
    "v007": {
      "candidate_win_rate": 0.35,
      "delta_win_rate": -0.05,
      "games": 20,
      "reference_win_rate": 0.4
    },
    "v008": {
      "candidate_win_rate": 0.1,
      "delta_win_rate": 0.0,
      "games": 20,
      "protected_heuristic_guard": true,
      "reference_win_rate": 0.1
    }
  }
}
```

## Promotion

```json
{}
```

## Tests fixes

```json
{
  "command": "poetry run pytest -q",
  "duration_seconds": 41.654,
  "exit_code": 0,
  "timed_out": false
}
```

## Décision déterministe

```json
{
  "accepted": false,
  "deltas": {
    "neural:v002": -0.1,
    "random": 0.1,
    "v007": -0.05,
    "v008": 0.0
  },
  "mean_delta_win_rate": -0.0125,
  "minimum_mean_delta": 0.0,
  "opponent_count": 4,
  "performance_followup_required": false,
  "performance_gate": {
    "accepted": true,
    "available": true,
    "max_regression": -0.013172067701289227,
    "threshold": 0.05
  },
  "reason": "v008_guard_or_mean_gain_failed",
  "v008_floor": 0.0
}
```

## Performance

```json
{
  "baseline": {
    "checkpoint": "configs/neural_profiles/v002.pt",
    "elapsed_seconds": 26.22684347795439,
    "neural_inference_seconds": 9.819989162817365,
    "throughput_games_per_second": 0.762694118151328,
    "total_actions": 6668
  },
  "candidate": {
    "checkpoint": "artifacts/neural_training/checkpoint.pt",
    "elapsed_seconds": 25.88138172007166,
    "neural_inference_seconds": 9.711815259419382,
    "throughput_games_per_second": 0.772758294146805,
    "total_actions": 7057
  },
  "relative_elapsed_change": -0.01315728145374026
}
```

## Performance gate

```json
{
  "accepted": true,
  "available": true,
  "max_regression": -0.013172067701289227,
  "threshold": 0.05
}
```

## Suivi performance obligatoire

Non renseigné

## Analyse humaine

```json
{
  "causal_attribution": "The candidate was initialized from v002 and changed only gamma in the training profile; the partial budget limits causal strength, and the required panel rejects it.",
  "future_ideas": [
    "Instrument one PPO rollout with terminal outcome counts, advantage variance and gradient norms.",
    "Compare gamma=0.995 and gamma=1.0 only after an equal resumable budget is completed.",
    "Keep requiring Random, v007, v008 and neural v002 validation plus comparable runtime."
  ],
  "result": "The gamma change yielded an isolated Random gain but did not generalize to v007 or the active neural reference. The PPO campaign stopped after one saved update, so no full 128-game claim or promotion is justified."
}
```

## Erreur

v008_guard_or_mean_gain_failed

## Limites et suite

À compléter par l'agent ou l'analyse humaine.
