# Expérience exp-00040

## Décision

rejected

## Hypothèse

Un fine-tuning selectif depuis v002, sur un split par partie et cible sur buy_card, recruit_mercenary et banish_card, ameliorera la force sans deplacer fortement la politique active.

## Évolution du catalogue d'idées

diff --git a/doc/Ideas.md b/doc/Ideas.md
index 8452c5b..6dcd81d 100644
--- a/doc/Ideas.md
+++ b/doc/Ideas.md
@@ -456,6 +456,31 @@ une piste existante.
 
 ## Pistes générales à explorer
 
+## Expérience exp-00040 — rejetée
+
+- [Terminé] Tester un fine-tuning sélectif depuis v002 sur `12 105` décisions de `82` parties,
+  avec split déterministe par `game_id` (`10 318` train, `829` holdout), et pondération `1,25` des
+  actions `buy_card`, `recruit_mercenary` et `banish_card`. La candidate a été initialisée par les
+  poids v002 avec Adam réinitialisé; le moteur, les heuristiques et le masque sont restés inchangés.
+- [Résultat] Le holdout atteint `89,14 %` top-1 et `92,63 %` pairwise, mais le screening apparié de
+  `20` parties donne Random `+10,0` points, v007 `-15,0`, v008 `0,0` et v002 `+5,0` (référence
+  neural v002). Le benchmark comparable de `50` parties contre v002 passe de `9,7297 s` à
+  `11,0499 s` (`-13,57 %` de débit), avec `17 214` contre `17 240` actions.
+- [Supprimé] Ne pas promouvoir la candidate ni reprendre la pondération sélective sans corriger la
+  faiblesse v007 et le ralentissement. Les panels demandés de `100` et `200` parties n'ont pas
+  produit de sortie exploitable dans la fenêtre de l'orchestrateur; aucun statut accepté n'en est
+  déduit.
+
+## Suites issues d'exp-00040
+
+- [À privilégier] Construire le prochain dataset avec un holdout par partie effectivement séparé et
+  un filtrage par marge teacher vérifié sur chaque alternative légale, puis ablater la pondération
+  des actions rares au lieu de la conserver implicitement.
+- [À étudier] Comparer les erreurs holdout de `v007` et Random par action avant tout entraînement,
+  en particulier les décisions `play_card` qui peuvent expliquer le compromis observé.
+- [À supprimer] Toute promotion fondée sur la seule accuracy offline, le screening de 20 parties,
+  ou un gain contre v008 sans non-régression simultanée contre v007 et la référence v002.
+
 Ces pistes sont volontairement larges. Elles ne constituent pas un plan imposé : l'agent peut les
 décomposer, les combiner avec prudence, les reformuler après une analyse offline ou les supprimer
 si les rapports montrent qu'elles n'ont plus de sens. Chaque expérience doit toutefois isoler une

## Provenance

```json
{
  "budget_seconds": 3600,
  "campaign_id": "campaign-20260806-103704-ed9261",
  "dataset": "/tmp/exp00040_raw.jsonl",
  "dataset_records": 12105,
  "dataset_sha256": "c3e5637d53b8ab69f9667305c26325f063a8d1f7ab0045927422aa302c7a7f34",
  "experiment_family": "other",
  "experiment_kind": "quality",
  "novelty": "Premier essai combinant un split deterministe par game_id et un holdout explicite avec une ponderation ciblee des actions rares; il corrige l'absence de holdout d'exp-00036/37 et n'est pas un lot PLAY-only comme exp-00038.",
  "parent_commit": "a48f8e1dc591c23633009a893ed24d210eaa08aa",
  "parent_profile": "v002",
  "seed": 104,
  "teacher_profile": "configs/heuristic_profiles/v008.yaml"
}
```

## Changements autorisés

```json
[
  "doc/Ideas.md",
  "result.json"
]
```

## Recette d'entraînement

```json
{
  "epochs": 1,
  "initial_checkpoint": "configs/neural_profiles/v002.pt",
  "learning_rate": 1e-05,
  "optimizer": "Adam",
  "reset_optimizer": true,
  "split": "train by game_id, seed 40040",
  "strategic_actions": [
    "buy_card",
    "recruit_mercenary",
    "banish_card"
  ],
  "strategic_weight": 1.25,
  "torch_threads": 1
}
```

## Commandes

```json
[
  "codex exec --sandbox workspace-write --ephemeral -",
  "poetry run pytest -q"
]
```

## Résultats offline

```json
{}
```

## Observations analytiques

```json
{}
```

## Limites de l'analyse

```json
{}
```

## Recommandations

```json
{}
```

## Résultats de screening

```json
{}
```

## Validation

```json
{
  "decision": "rejected",
  "protocol_note": "The 100- and 200-game validator attempts produced no output in the orchestrator window; these are the completed 20-game paired results and are not a promotion gate.",
  "results": {
    "random": {
      "candidate_win_rate": 1.0,
      "delta_win_rate": 0.1,
      "games": 20,
      "reference_win_rate": 0.9
    },
    "v007": {
      "candidate_win_rate": 0.4,
      "delta_win_rate": -0.15,
      "games": 20,
      "reference_win_rate": 0.55
    },
    "v008": {
      "candidate_win_rate": 0.25,
      "delta_win_rate": 0.0,
      "games": 20,
      "reference_win_rate": 0.25
    }
  }
}
```

## Promotion

```json
{}
```

## Tests fixes

```json
{
  "command": "poetry run pytest -q",
  "duration_seconds": 12.137,
  "exit_code": 0,
  "timed_out": false
}
```

## Décision déterministe

```json
{
  "accepted": false,
  "deltas": {
    "random": 0.1,
    "v007": -0.15,
    "v008": 0.0
  },
  "mean_delta_win_rate": -0.016666666666666663,
  "minimum_mean_delta": 0.0,
  "opponent_count": 3,
  "performance_followup_required": true,
  "performance_gate": {
    "accepted": false,
    "available": true,
    "max_regression": 0.1356894190491803,
    "threshold": 0.05
  },
  "reason": "v008_guard_or_mean_gain_failed",
  "v008_floor": 0.0
}
```

## Performance

```json
{
  "baseline": {
    "actions": 17240,
    "checkpoint": "configs/neural_profiles/v002.pt",
    "elapsed_seconds": 9.729663191130385,
    "games": 50,
    "opponent": "v002",
    "seed": 40040
  },
  "candidate": {
    "actions": 17214,
    "checkpoint": "/tmp/exp00040_candidate.pt",
    "elapsed_seconds": 11.04987553707906,
    "games": 50,
    "opponent": "v002",
    "seed": 40040
  },
  "elapsed_delta_percent": -13.565142999999997
}
```

## Performance gate

```json
{
  "accepted": false,
  "available": true,
  "max_regression": 0.1356894190491803,
  "threshold": 0.05
}
```

## Suivi performance obligatoire

Non renseigné

## Analyse humaine

```json
{
  "active_reference_preserved": "v002",
  "conclusion": "Rejected: the candidate improved Random in the small completed panel but regressed v007, did not improve v008, lost the comparable v002 benchmark, and was 13.57% slower.",
  "future_ideas": [
    "Verify teacher margins against all legal alternatives before filtering",
    "Measure holdout errors by action and opponent matchup",
    "Ablate rare-action weighting before another training run"
  ]
}
```

## Erreur

v008_guard_or_mean_gain_failed

## Limites et suite

À compléter par l'agent ou l'analyse humaine.
