# Expérience exp-00050

## Décision

rejected

## Hypothèse

A semantic residual initialized as the identity, added to v002 card embeddings while preserving v002 global candidate context, will improve card-sensitive playing strength without materially changing comparable runtime.

## Évolution du catalogue d'idées

diff --git a/doc/Ideas.md b/doc/Ideas.md
index cb392b8..8538ce2 100644
--- a/doc/Ideas.md
+++ b/doc/Ideas.md
@@ -100,3 +100,25 @@ pas devenir une nouvelle série d'analyses descriptives sans décision associée
 
 Toute nouvelle idée doit préciser l'axe choisi (architecture/représentation ou training), une
 hypothèse falsifiable, la référence v002, les métriques attendues et la condition de rejet.
+
+## Expérience exp-00050 — rejetée
+
+- [Terminé] Tester une variation d’architecture `semantic_identity_v3` depuis v002 : conserver le
+  contexte global des actions de v002 et ajouter un résidu sémantique linéaire sur les embeddings de
+  cartes, initialisé à l’identité. Le dataset a été régénéré avec v008 et v007, séparé par `game_id`.
+- [Résultat] La validation officielle de 100 parties par adversaire donne Random `-3,0` points,
+  v007 `-3,0`, v008 `+5,0` et v002 neural `-12,0`. Le benchmark comparable médian passe de `14,2108 s`
+  à `14,3520 s` (`-0,99 %` de débit) et de `17 073` à `16 667` actions.
+- [Supprimé] Ne pas promouvoir la candidate : le gain contre la garde v008 ne compense pas les
+  régressions Random, v007 et surtout v002. Le résidu sémantique est une variation effective, mais
+  son transfert partiel ne suffit pas à préserver la politique active.
+
+## Suites issues d'exp-00050
+
+- [À privilégier] Mesurer avant entraînement la dérive de politique induite par le résidu identité,
+  puis tester une version gelant ce résidu pendant le premier passage ou limitant explicitement sa
+  norme ; comparer d’abord directement à v002 avant la validation complète.
+- [À étudier] Évaluer séparément l’effet de la représentation sur les erreurs `play_card` par carte
+  et sur `recruit_mercenary`, avec un budget de décisions modifiées et un holdout par phase/action.
+- [À supprimer] Toute promotion d’une représentation qui améliore seulement v008 ou la précision
+  offline sans non-régression contre Random, v007 et v002.

## Provenance

```json
{
  "budget_seconds": 3600,
  "campaign_id": "campaign-20260806-124614-2706ab",
  "dataset": "/tmp/shards-ai-exp-00050/dataset.jsonl",
  "dataset_records": 12010,
  "dataset_sha256": "4a03b573977745b24ed8914d410a1165b443e93128a85eb410c0a950bcbaacb0",
  "experiment_family": "architecture",
  "experiment_kind": "quality",
  "novelty": "First effective representation change after the rejected local imitation objectives: a trainable identity-initialized semantic residual was added to the existing v002 contextual scorer, with compatible v002 weights transferred and only two new tensors initialized.",
  "parent_commit": "1aa41c9b16287f4e055957c675aab6e4768e0a21",
  "parent_profile": "v002",
  "seed": 104,
  "teacher_profile": "v008"
}
```

## Changements autorisés

```json
[
  "doc/Ideas.md",
  "shards_ai/ai/neural_model.py",
  "result.json"
]
```

## Recette d'entraînement

```json
{
  "epochs": 1,
  "initial_checkpoint": "configs/neural_profiles/v002.pt",
  "learning_rate": 1e-05,
  "method": "imitation",
  "optimizer": "Adam",
  "reset_optimizer": true,
  "seed": 50051,
  "split": "game_id split_seed=50050, train=8923, validation=2126",
  "torch_threads": 1,
  "transfer": "22/24 compatible tensors copied from v002; semantic_residual.weight and semantic_residual.bias initialized to identity/zero"
}
```

## Commandes

```json
[
  "codex exec --sandbox workspace-write --ephemeral -",
  "poetry run pytest -q"
]
```

## Résultats offline

```json
{}
```

## Observations analytiques

```json
{}
```

## Limites de l'analyse

```json
{}
```

## Recommandations

```json
{}
```

## Résultats de screening

```json
{}
```

## Validation

```json
{
  "batch_games": 20,
  "games_per_opponent": 100,
  "protocol": "validate_neural_profile_batched.py, 100 games per opponent, batch_games=20, progress-output enabled, identical seeds, active reference v002",
  "reference_profile": "v002",
  "results": {
    "neural:v002": {
      "candidate_win_rate": 0.52,
      "delta_win_rate": -0.12,
      "games": 100,
      "reference_win_rate": 0.64
    },
    "random": {
      "candidate_win_rate": 0.88,
      "delta_win_rate": -0.03,
      "games": 100,
      "reference_win_rate": 0.91
    },
    "v007": {
      "candidate_win_rate": 0.42,
      "delta_win_rate": -0.03,
      "games": 100,
      "reference_win_rate": 0.45
    },
    "v008": {
      "candidate_win_rate": 0.2,
      "delta_win_rate": 0.05,
      "games": 100,
      "protected_heuristic_guard": true,
      "reference_win_rate": 0.15
    }
  }
}
```

## Promotion

```json
{}
```

## Tests fixes

```json
{
  "command": "poetry run pytest -q",
  "duration_seconds": 16.204,
  "exit_code": 0,
  "timed_out": false
}
```

## Décision déterministe

```json
{
  "accepted": false,
  "deltas": {
    "neural:v002": -0.12,
    "random": -0.03,
    "v007": -0.03,
    "v008": 0.05
  },
  "mean_delta_win_rate": -0.0325,
  "minimum_mean_delta": 0.0,
  "opponent_count": 4,
  "performance_followup_required": false,
  "performance_gate": {
    "accepted": true,
    "available": true,
    "max_regression": 0.009923952912714274,
    "threshold": 0.05
  },
  "reason": "v008_guard_or_mean_gain_failed",
  "v008_floor": 0.0
}
```

## Performance

```json
{
  "baseline": {
    "checkpoint": "configs/neural_profiles/v002.pt",
    "elapsed_seconds": 14.210825384943746,
    "games": 50,
    "opponent": "v002",
    "repetitions": 3,
    "seed": 50050,
    "throughput_games_per_second": 3.518087,
    "total_actions": 17073
  },
  "candidate": {
    "checkpoint": "/tmp/shards-ai-exp-00050/semantic-candidate.pt",
    "elapsed_seconds": 14.351852946914732,
    "games": 50,
    "opponent": "v002",
    "repetitions": 3,
    "seed": 50050,
    "throughput_games_per_second": 3.484867,
    "total_actions": 16667
  },
  "elapsed_delta_percent": -0.992,
  "throughput_delta_percent": -0.944
}
```

## Performance gate

```json
{
  "accepted": true,
  "available": true,
  "max_regression": 0.009923952912714274,
  "threshold": 0.05
}
```

## Suivi performance obligatoire

Non renseigné

## Analyse humaine

```json
{
  "changed_files": [
    "shards_ai/ai/neural_model.py",
    "doc/Ideas.md",
    "result.json"
  ],
  "conclusion": "Rejected: the effective representation change improved only the protected v008 matchup and regressed all active-quality references, especially v002.",
  "future_ideas": [
    "Measure policy drift from the identity residual before training.",
    "Try a norm-limited or initially frozen residual with phase/action/card holdout attribution.",
    "Reject representation candidates that improve v008 alone."
  ],
  "rejection_reason": "v002, Random and v007 non-regression failed; runtime was also 0.99% slower in elapsed time.",
  "tests": {
    "command": "env PYTHONPATH=. poetry run pytest -q",
    "failed": 0,
    "passed": 307
  }
}
```

## Erreur

v008_guard_or_mean_gain_failed

## Limites et suite

À compléter par l'agent ou l'analyse humaine.
