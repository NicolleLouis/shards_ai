# Expérience exp-00051

## Décision

rejected

## Hypothèse

A zero-initialized semantic residual bounded to 0.1 of the v002 card embedding, trained for one imitation epoch from converted v002 weights, will improve card-sensitive playing strength without regressing the active neural policy or comparable runtime.

## Évolution du catalogue d'idées

diff --git a/doc/Ideas.md b/doc/Ideas.md
index 8538ce2..bfff404 100644
--- a/doc/Ideas.md
+++ b/doc/Ideas.md
@@ -122,3 +122,28 @@ hypothèse falsifiable, la référence v002, les métriques attendues et la cond
   et sur `recruit_mercenary`, avec un budget de décisions modifiées et un holdout par phase/action.
 - [À supprimer] Toute promotion d’une représentation qui améliore seulement v008 ou la précision
   offline sans non-régression contre Random, v007 et v002.
+
+## Expérience exp-00051 — rejetée
+
+- [Terminé] Tester une correction bornée de `semantic_identity_v3` : conserver le scorer contextuel
+  de v002, ajouter un résidu linéaire sémantique initialisé à zéro et limiter sa contribution à
+  `0,1`, avec conversion explicite des poids v002 et Adam réinitialisé.
+- [Résultat] Le panel officiel de 100 parties par adversaire donne Random `-10,0` points, v007
+  `-1,0`, v008 `+0,0` et v002 neural `-7,0`. Le gain contre v001 (`+11,0`) ne constitue pas une
+  preuve de force contre les références actives.
+- [Résultat] Le benchmark comparable médian sur 50 parties contre v002 passe de `14,2984 s` et
+  `17 073` actions pour v002 à `10,7607 s` et `17 013` actions pour la candidate ; cette mesure
+  candidate est très variable entre répétitions et ne compense pas la régression de qualité.
+- [Supprimé] Ne pas promouvoir le résidu borné, ni reprendre une variation de représentation sans
+  corriger la dérive de politique observée contre Random et v002.
+
+## Suites issues d'exp-00051
+
+- [À privilégier] Avant tout nouvel entraînement, mesurer la dérive décisionnelle de la candidate
+  par phase et type d’action sur un holdout indépendant ; isoler les décisions changées plutôt que
+  modifier encore l’amplitude du résidu.
+- [À étudier] Tester une régularisation explicite des logits ou une contrainte de conservation de
+  l’action v002 sur les états hors slices ciblées, avec budget de décisions modifiées et validation
+  complète contre Random, v007, v008 et v002.
+- [À supprimer] Les résidus bornés ou gains de runtime acceptés malgré une forte baisse contre
+  Random/v002 ; ne pas utiliser v001 seul comme critère de sélection.

## Provenance

```json
{
  "budget_seconds": 3600,
  "campaign_id": "campaign-20260806-124614-2706ab",
  "dataset": "/tmp/shards-ai-exp-00050/dataset.jsonl",
  "dataset_records": 12010,
  "dataset_sha256": "4a03b573977745b24ed8914d410a1165b443e93128a85eb410c0a950bcbaacb0",
  "experiment_family": "architecture",
  "experiment_kind": "quality",
  "novelty": "A concrete correction of exp-00050: preserve v002 global candidate context, add only a zero-initialized linear semantic residual, and bound its contribution instead of exposing an unrestricted representation change.",
  "parent_commit": "44fa1ec962a321f4307a182d0f8bc7e7e197c91c",
  "parent_profile": "v002",
  "seed": 104,
  "teacher_profile": "v008"
}
```

## Changements autorisés

```json
[
  "doc/Ideas.md",
  "shards_ai/ai/neural_model.py",
  "configs/neural_training_profiles/candidates/v004-bounded-residual.yaml",
  "doc/Experiments/exp-00051.md",
  "result.json"
]
```

## Recette d'entraînement

```json
{
  "architecture": "bounded_semantic_residual_v4",
  "dataset_split": "game_id",
  "epochs": 1,
  "initial_checkpoint": "configs/neural_profiles/v002.pt",
  "learning_rate": 1e-05,
  "method": "imitation",
  "optimizer": "Adam",
  "reset_optimizer": true,
  "residual_scale": 0.1,
  "seed": 50052,
  "split_seed": 50050,
  "torch_threads": 1
}
```

## Commandes

```json
[
  "codex exec --sandbox workspace-write --ephemeral -",
  "poetry run pytest -q"
]
```

## Résultats offline

```json
{}
```

## Observations analytiques

```json
{}
```

## Limites de l'analyse

```json
{}
```

## Recommandations

```json
{}
```

## Résultats de screening

```json
{}
```

## Validation

```json
{
  "protocol": "validate_neural_profile_batched.py --games 100 --batch-games 20 --progress-output, identical seeds, active latest neural reference v002",
  "results": {
    "neural:v002": {
      "delta_win_rate": -0.07
    },
    "random": {
      "delta_win_rate": -0.1
    },
    "v007": {
      "delta_win_rate": -0.01
    },
    "v008": {
      "delta_win_rate": 0.0
    }
  }
}
```

## Promotion

```json
{}
```

## Tests fixes

```json
{
  "command": "poetry run pytest -q",
  "duration_seconds": 11.63,
  "exit_code": 0,
  "timed_out": false
}
```

## Décision déterministe

```json
{
  "accepted": false,
  "deltas": {
    "neural:v002": -0.07,
    "random": -0.1,
    "v007": -0.01,
    "v008": 0.0
  },
  "mean_delta_win_rate": -0.045,
  "minimum_mean_delta": 0.0,
  "opponent_count": 4,
  "performance_followup_required": false,
  "performance_gate": {
    "accepted": true,
    "available": true,
    "max_regression": -0.24741915377499046,
    "threshold": 0.05
  },
  "reason": "v008_guard_or_mean_gain_failed",
  "v008_floor": 0.0
}
```

## Performance

```json
{
  "baseline": {
    "checkpoint": "configs/neural_profiles/v002.pt",
    "elapsed_seconds": 14.298434065945912,
    "games": 50,
    "repetitions": 3,
    "throughput_games_per_second": 3.4968864261215344,
    "total_actions": 17073
  },
  "candidate": {
    "checkpoint": "/tmp/shards-ai-exp-00051-vc5ku87d-v004-candidate.pt",
    "elapsed_seconds": 10.760727609042078,
    "elapsed_seconds_runs": [
      14.83628612695611,
      10.760727609042078,
      9.399995011946885
    ],
    "games": 50,
    "repetitions": 3,
    "runtime_note": "Candidate timings were materially variable; median is reported and is not treated as a quality gain.",
    "throughput_games_per_second": 4.646525942909823,
    "total_actions": 17013
  },
  "workload": "50 games, seed 50050, candidate/reference neural v002, torch_threads=1, three repetitions, median"
}
```

## Performance gate

```json
{
  "accepted": true,
  "available": true,
  "max_regression": -0.24741915377499046,
  "threshold": 0.05
}
```

## Suivi performance obligatoire

Non renseigné

## Analyse humaine

```json
{
  "conclusion": "Rejected. The bounded residual did not preserve playing strength: it lost 10 points against Random, 1 point against v007, and 7 points against active neural v002. The neutral v008 guard and +11 points against v001 are insufficient evidence of improvement.",
  "decision_reason": "active_quality_non_regression_failed",
  "future_ideas": [
    "Measure policy drift by phase and action type on an independent holdout before another training update.",
    "Test explicit v002 logit or action-preservation regularization only on non-target states, with a fixed changed-decision budget.",
    "Reject representation candidates that improve v001 or v008 while regressing Random or v002."
  ],
  "runtime_interpretation": "The candidate median was faster and produced 60 fewer actions, but timings were noisy and runtime cannot compensate for the quality regressions."
}
```

## Erreur

v008_guard_or_mean_gain_failed

## Limites et suite

À compléter par l'agent ou l'analyse humaine.
