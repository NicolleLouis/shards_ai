# Expérience exp-00077

## Décision

accepted

## Hypothèse

A phase/action/legal-set balanced visible holdout will show whether the previously PLAY-heavy v002 diagnosis survives reweighting and whether its confident disagreements are calibration-localized rather than only coverage artifacts.

## Évolution du catalogue d'idées

diff --git a/doc/Ideas.md b/doc/Ideas.md
index 6d9e434..6a84736 100644
--- a/doc/Ideas.md
+++ b/doc/Ideas.md
@@ -192,6 +192,31 @@ refuser toute conclusion fondée sur une amélioration contre v008 ou v002 neura
 - [À étudier] Mesurer le coût de validation offline sur le dataset complet et conserver un holdout
   par `game_id` explicitement matérialisé pour éviter les campagnes longues non reprenables.
 
+## Expérience exp-00077 — analyse terminée
+
+- [Terminé] Refaire le diagnostic sur 64 parties et 15 396 décisions visibles, avec v002, son parent
+  v001 et les enseignants v007/v008 ; ajouter macro-accuracy phase×action, cardinalité légale,
+  ECE/Brier et états représentatifs.
+- [Résultat] Après rééquilibrage descriptif, v002 garde une loss meilleure (`1,1806` contre `1,3079`)
+  mais une macro-accuracy plus faible (`68,96 %` contre `71,06 %`). `gain_mastery` reste à `48,70 %`
+  avec ECE `0,531`, `recruit_mercenary` à `23,87 %`, et l'accuracy tombe à `60,93 %` sur sept
+  actions légales.
+- [Conclusion] La faiblesse n'est pas seulement un artefact PLAY-heavy ; elle persiste dans les
+  slices équilibrées et est particulièrement calibrée sur `gain_mastery`/grands ensembles légaux.
+  Aucun checkpoint, moteur, heuristique ou masque n'a été modifié.
+- [Rapport] Les détails sont dans `doc/Experiments/exp-00077.md`; le dataset et les états bruts restent
+  temporaires hors dépôt.
+
+## Nouvelles idées après exp-00077
+
+- [À privilégier] Tester une correction bornée et conservatrice de `gain_mastery`/`recruit_mercenary`
+  avec budget d'argmax fixé avant sélection, protection exacte de v002 hors slice et holdout
+  `game_id` séparé.
+- [À étudier] Calibrer séparément les décisions selon la taille de l'ensemble légal, sans modifier
+  les logits bruts globalement ni pondérer les cartes rares sous un seuil de couverture.
+- [À supprimer] Toute nouvelle analyse globale PLAY-heavy sans macro-métriques phase×action, ECE et
+  cardinalité légale ; elle répéterait exp-00072 sans réduire l'incertitude.
+
 ## Pistes abandonnées
 
 - [À supprimer] Pondération globale `play_card`, conservation globale des logits, mélange DAgGER divergent ou calibration globale sans attribution causale.

## Provenance

```json
{
  "budget_seconds": 3600,
  "campaign_id": "campaign-20260806-224834-dfdd90",
  "dataset": "/tmp/exp00077-visible-holdout.jsonl",
  "dataset_records": 15396,
  "dataset_sha256": "4d1784546c29a982c20258991059197b3f5cb70c0be6f6a943a7c32feefcdfc9",
  "experiment_family": "analysis",
  "experiment_kind": "analysis",
  "novelty": null,
  "parent_commit": "4ddb5b925ec60f7fabbbbee91d9aa83f64aef008",
  "parent_profile": "v002",
  "seed": 104,
  "teacher_profile": [
    "v007",
    "v008"
  ]
}
```

## Changements autorisés

```json
[
  "doc/Ideas.md",
  "result.json",
  "doc/Experiments/exp-00077.md"
]
```

## Recette d'entraînement

```json
{
  "games": 64,
  "matchups": [
    "v007-random",
    "v008-random",
    "v007-v008",
    "v008-v007"
  ],
  "observation_source": "NeuralObservation and serialized visible legal action representations only",
  "seed": 77077,
  "torch_threads": 1,
  "trained": false
}
```

## Commandes

```json
[
  "codex exec --sandbox workspace-write --ephemeral -",
  "poetry run pytest -q"
]
```

## Résultats offline

```json
{}
```

## Observations analytiques

```json
{}
```

## Limites de l'analyse

```json
{}
```

## Recommandations

```json
{}
```

## Résultats de screening

```json
{}
```

## Validation

```json
{}
```

## Promotion

```json
{}
```

## Tests fixes

```json
{
  "command": "poetry run pytest -q",
  "duration_seconds": 11.908,
  "exit_code": 0,
  "timed_out": false
}
```

## Décision déterministe

```json
{
  "analysis_complete": true,
  "subject_profile": "v002"
}
```

## Performance

```json
{}
```

## Performance gate

```json
{}
```

## Suivi performance obligatoire

Non renseigné

## Analyse humaine

```json
{
  "limitations": [
    "v007/v008 choices are heuristic labels, not optimal actions.",
    "The holdout is diagnostic, not promotion validation; rare card slices remain uncertain.",
    "ECE is calculated per legal set and depends on the selected bins and sample."
  ],
  "novelty": "First diagnostic after exp-00072 to report phase-action macro metrics with an explicit legal-action-count stratum and ECE/Brier on a fresh multi-matchup holdout.",
  "observations": [
    "All metrics use only masked observations and visible legal actions.",
    "v001/v002 raw logits are not compared across checkpoints; confidence is softmax-normalized within each legal set.",
    "The balanced metrics are descriptive and do not imply a causal win-rate effect."
  ],
  "recommendations": [
    "Use eligible phase-action slices and legal-set strata to define any future bounded correction.",
    "Do not weight rare cards until they exceed a predeclared coverage threshold.",
    "Preserve v002 argmax outside an attributed slice and validate any correction against the full panel."
  ]
}
```

## Limites et suite

À compléter par l'agent ou l'analyse humaine.
