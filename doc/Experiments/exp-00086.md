# Expérience exp-00086

## Décision

rejected

## Hypothèse

Prebuilding fixed action, phase, and target vocabulary indexes will reduce repeated linear vocabulary lookups during neural action encoding without changing model outputs.

## Évolution du catalogue d'idées

diff --git a/doc/Ideas.md b/doc/Ideas.md
index ed0a464..843c510 100644
--- a/doc/Ideas.md
+++ b/doc/Ideas.md
@@ -396,3 +396,22 @@ et le nombre d'actions restent des métriques secondaires et ne peuvent pas comp
 - [À étudier] Curriculum phase/action prédéclaré sur le split `game_id`, avec dérive d'argmax V2
   mesurée avant sélection.
 - [À supprimer] Toute promotion fondée sur le gain Random, le top-1 offline ou le runtime seul.
+
+## Expérience exp-00086 — index des vocabulaires d'inférence — rejetée
+
+- [Terminé] Tester une optimisation purement runtime du scorer neural actif : remplacer les
+  recherches linéaires des vocabulaires d'action, phase et cible par des index préconstruits.
+- [Résultat] Les trajectoires sont identiques sur 100 parties, mais la médiane passe de `17,3474 s`
+  à `17,5153 s` sur le même checkpoint V2, seed et benchmark ; gain de débit `-0,96 %`.
+- [Supprimé] Le patch candidat a été retiré ; aucune modification de code ne reste issue de cette
+  expérience. La gate performance est rejetée et la campagne s'arrête sur cette passe plus lente.
+
+## Nouvelles idées après exp-00086
+
+- [À privilégier] Reprofiler le coût d'inférence avec une séparation explicite représentation,
+  encodage d'état et score avant toute nouvelle micro-optimisation, en conservant le benchmark
+  neural V2 contre V2 et trois répétitions médianes.
+- [À étudier] Tester uniquement une réduction d'allocations démontrée par ce profil, sans cache
+  supplémentaire, changement de modèle, moteur, heuristique ou masque d'information.
+- [À supprimer] Les index de vocabulaires seuls : ils ont été mesurés et ralentissent le workload
+  contrôlé de `0,96 %`.

## Provenance

```json
{
  "budget_seconds": 3600,
  "campaign_id": "campaign-20260806-224834-dfdd90",
  "dataset": null,
  "dataset_records": null,
  "dataset_sha256": null,
  "experiment_family": "performance",
  "experiment_kind": "performance",
  "novelty": "A runtime-only inference micro-optimization distinct from exp-00068's accepted embedding cache; no model, checkpoint, training, dataset, engine, heuristic, or information-mask change.",
  "parent_commit": "db83f2a62642d04ecf166fdd568ec2061c17798b",
  "parent_profile": "v003",
  "seed": 104,
  "teacher_profile": null
}
```

## Changements autorisés

```json
[
  "doc/Ideas.md",
  "result.json",
  "doc/Experiments/exp-00086.md"
]
```

## Recette d'entraînement

```json
{}
```

## Commandes

```json
[
  "codex exec --sandbox workspace-write --ephemeral -",
  "poetry run pytest -q"
]
```

## Résultats offline

```json
{}
```

## Observations analytiques

```json
{}
```

## Limites de l'analyse

```json
{}
```

## Recommandations

```json
{}
```

## Résultats de screening

```json
{}
```

## Validation

```json
{}
```

## Promotion

```json
{}
```

## Tests fixes

```json
{
  "command": "poetry run pytest -q",
  "duration_seconds": 11.979,
  "exit_code": 0,
  "timed_out": false
}
```

## Décision déterministe

```json
{
  "accepted": true,
  "available": true,
  "max_regression": 0.009681420559628888,
  "threshold": 0.02
}
```

## Performance

```json
{
  "baseline": {
    "checkpoint": "configs/neural_profiles/v002.pt",
    "elapsed_seconds": 17.347377288155258,
    "games": 100,
    "repetitions": 3,
    "seed": 0,
    "throughput_games_per_second": 5.764072708477006
  },
  "candidate": {
    "checkpoint": "configs/neural_profiles/v002.pt",
    "elapsed_seconds": 17.515324543288443,
    "games": 100,
    "repetitions": 3,
    "seed": 0,
    "throughput_games_per_second": 5.709368505107129
  },
  "gate": {
    "accepted": false,
    "reason": "candidate median elapsed time is 0.9685% slower",
    "threshold_percent": 2.0
  }
}
```

## Performance gate

```json
{
  "accepted": true,
  "available": true,
  "max_regression": 0.009681420559628888,
  "threshold": 0.02
}
```

## Suivi performance obligatoire

Non renseigné

## Analyse humaine

```json
{
  "attribution": "The candidate changed only vocabulary lookup implementation; identical per-seed trajectories and decision/action counts show that the measured difference is runtime overhead from this code change.",
  "commands": {
    "benchmark": "PYTHONPATH=. poetry run python benchmarks/benchmark_neural_players.py --checkpoint configs/neural_profiles/v002.pt --opponent neural --opponent-checkpoint configs/neural_profiles/v002.pt --games 100 --seed 0 --torch-threads 1",
    "tests": "PYTHONPATH=. poetry run pytest -q tests/ai/test_neural_model.py tests/ai/test_neural_player.py",
    "tests_result": "13 passed"
  },
  "follow_up": "Profile representation, state encoding, and scoring separately before another runtime-only change.",
  "quality_validation": "not run and not used as evidence",
  "result": "Rejected because the performance-only gate requires a faster candidate and the median benchmark regressed."
}
```

## Erreur

performance gain below the robust 2% threshold

## Limites et suite

À compléter par l'agent ou l'analyse humaine.
