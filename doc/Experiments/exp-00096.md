# Expérience exp-00096

## Décision

accepted

## Hypothèse

La dérive du profil actif v003 par rapport à v002 est localisée dans des slices observables par phase, action, carte et cardinalité légale.

## Évolution du catalogue d'idées

diff --git a/doc/Ideas.md b/doc/Ideas.md
index 11f4334..f08f17f 100644
--- a/doc/Ideas.md
+++ b/doc/Ideas.md
@@ -642,3 +642,23 @@ et le nombre d'actions restent des métriques secondaires et ne peuvent pas comp
   `game_id` et le même budget, avec sélection sur holdout indépendant avant la gate longue.
 - [À supprimer] Les réductions de capacité sélectionnées sur le seul runtime ou le top-1 ; exp-00095
   montre qu'elles peuvent améliorer Random/v007 tout en régressant la garde v008.
+
+## Expérience exp-00096 — diagnostic de dérive V3/V2 — terminée
+
+- [Terminé] Holdout séparé de 90 parties et 15 193 décisions visibles contre `v007`, `v008` et
+  `Random`, avec loss/accuracy vers l'argmax `v002`, couverture phase/action/carte/cardinalité,
+  comparaison de confiance et états représentatifs masqués.
+- [Résultat] La dérive `v003/v002` est de 14,28–16,74 % selon l'adversaire ; elle se concentre en
+  `play_card`, `activate_champion` et dans les ensembles de 4–8 actions. `infinity_shard` reste
+  instable, tandis que `drone_kiln` et `initie_de_l_ordre` sont plus stables sur ce protocole.
+- [Limite] Le diagnostic mesure une dérive de politique, pas une causalité de win-rate ; il ne
+  justifie ni entraînement ni promotion.
+
+## Nouvelles idées après exp-00096
+
+- [À privilégier] Tester une correction bornée sur une seule slice `play_card`/carte ou
+  `activate_champion`, avec budget d'argmax V3/V2 fixé avant sélection et holdout `game_id` séparé.
+- [À étudier] Augmenter la couverture visible de `recruit_mercenary`, `choose_pending_decision` et
+  des cartes rares avant toute pondération ; conserver des intervalles par slice.
+- [À supprimer] Toute nouvelle analyse globale sans ventilation par cardinalité légale et sans
+  séparation des trajectoires adversaires ; elle répéterait exp-00091.

## Provenance

```json
{
  "budget_seconds": 3600,
  "campaign_id": "campaign-20260806-224834-dfdd90",
  "dataset": null,
  "dataset_records": null,
  "dataset_sha256": null,
  "experiment_family": "analysis",
  "experiment_kind": "analysis",
  "novelty": null,
  "parent_commit": "43b062f8922d918f8d49cca86c579340a62e84d2",
  "parent_profile": "v003",
  "seed": 104,
  "teacher_profile": null
}
```

## Changements autorisés

```json
[
  "doc/Ideas.md",
  "result.json",
  "doc/Experiments/exp-00096.md"
]
```

## Recette d'entraînement

```json
{
  "checkpoint_modified": false,
  "games_per_opponent": 30,
  "observation_source": "NeuralObservation only",
  "opponents": [
    "v007",
    "v008",
    "random"
  ],
  "torch_threads": 1,
  "trained": false
}
```

## Commandes

```json
[
  "codex exec --sandbox workspace-write --ephemeral -",
  "poetry run pytest -q"
]
```

## Résultats offline

```json
{}
```

## Observations analytiques

```json
[
  "V003/V002 drift is consistent across Random, v007 and v008 trajectories and peaks against v008.",
  "Drift is concentrated in play_card, activate_champion and legal sets of 4-8 actions.",
  "infinity_shard is unstable in this sample; drone_kiln and initie_de_l_ordre are comparatively stable."
]
```

## Limites de l'analyse

```json
[
  "30 games per opponent is diagnostic, not quality validation.",
  "V002 is a parent reference, not ground truth.",
  "Rare slices and large legal sets have limited coverage.",
  "Normalized probabilities are intra-legal-set; raw logits are not cross-architecture comparable."
]
```

## Recommandations

```json
[
  "Predeclare an argmax-drift budget and a game_id holdout before any bounded correction.",
  "Test one protected slice at a time: play_card/card or activate_champion.",
  "Increase visible coverage of recruit_mercenary, choose_pending_decision and rare cards.",
  "Do not train or promote from this diagnostic alone."
]
```

## Résultats de screening

```json
{}
```

## Validation

```json
{}
```

## Promotion

```json
{}
```

## Tests fixes

```json
{
  "command": "poetry run pytest -q",
  "duration_seconds": 12.34,
  "exit_code": 0,
  "timed_out": false
}
```

## Décision déterministe

```json
{
  "analysis_complete": true,
  "subject_profile": "v003"
}
```

## Performance

```json
{}
```

## Performance gate

```json
{}
```

## Suivi performance obligatoire

Non renseigné

## Analyse humaine

```json
{
  "limitations": [
    "30 games per opponent is diagnostic, not quality validation.",
    "V002 is a parent reference, not ground truth.",
    "Rare slices and large legal sets have limited coverage.",
    "Normalized probabilities are intra-legal-set; raw logits are not cross-architecture comparable.",
    "States visited by V002 after its own actions were not sampled."
  ],
  "novelty": "Independent 90-game opponent-separated holdout extending exp-00091 with explicit card coverage and masked representative states.",
  "observations": [
    "V003/V002 drift is consistent across Random, v007 and v008 trajectories and peaks against v008.",
    "Drift is concentrated in play_card, activate_champion and legal sets of 4-8 actions.",
    "infinity_shard is unstable in this sample; drone_kiln and initie_de_l_ordre are comparatively stable.",
    "Representative disagreements are visible-state play/buy choices; no hidden-state or optimal-action label was used."
  ],
  "recommendations": [
    "Predeclare an argmax-drift budget and a game_id holdout before any bounded correction.",
    "Test one protected slice at a time: play_card/card or activate_champion.",
    "Increase visible coverage of recruit_mercenary, choose_pending_decision and rare cards.",
    "Do not train or promote from this diagnostic alone."
  ],
  "representative_states": [
    {
      "legal_actions": 6,
      "opponent_context": "random",
      "phase": "play",
      "turn": 2,
      "v2": "play_card:crystal",
      "v3": "play_card:crystal",
      "v3_confidence": 0.199965
    },
    {
      "legal_actions": 6,
      "opponent_context": "v007",
      "phase": "play",
      "turn": 12,
      "v2": "play_card:pirate_heretique",
      "v3": "play_card:sentinelle_des_tenebres",
      "v3_confidence": 0.523838
    },
    {
      "legal_actions": 6,
      "opponent_context": "v008",
      "phase": "play",
      "turn": 8,
      "v2": "play_card:drone_kiln",
      "v3": "play_card:zelote_des_epines",
      "v3_confidence": 0.372231
    }
  ]
}
```

## Limites et suite

À compléter par l'agent ou l'analyse humaine.
