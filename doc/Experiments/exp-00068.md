# Expérience exp-00068

## Décision

accepted

## Hypothèse

Caching the deterministic card embedding vectors between inference decisions will reduce repeated PyTorch embedding and semantic-encoder work without changing the checkpoint, training path, or observable game trajectory.

## Évolution du catalogue d'idées

diff --git a/doc/Ideas.md b/doc/Ideas.md
index ff59a67..9cfdc90 100644
--- a/doc/Ideas.md
+++ b/doc/Ideas.md
@@ -188,6 +188,26 @@ hypothèse falsifiable, la référence v002, les métriques attendues et la cond
   checkpoint avec absence d'effet de l'objectif.
 - [À conserver] Le contrôle v002/v002 et les métriques de runtime restent la référence comparable.
 
+## Expérience exp-00068 — performance — acceptée
+
+- [Terminé] Tester une cache des embeddings statiques de cartes dans `NeuralActionScorer`, active
+  uniquement en mode évaluation sans gradient ; l'entraînement conserve le chemin non caché.
+- [Résultat] Sur 50 parties v002 contre v002, seeds `0..49`, un thread PyTorch et trois répétitions,
+  la médiane passe de `14,7191 s` à `12,9886 s` (`-11,76 %`) et l'inférence de `5,7773 s` à
+  `4,9647 s` (`-14,07 %`). Les `17 587` actions et `8 770` décisions sont identiques.
+- [Conservé] Le gain est attribuable à la suppression des reconstructions répétées des embeddings
+  déterministes pendant les décisions ; aucun moteur, joueur heuristique, masque, checkpoint ou
+  objectif d'entraînement n'a été modifié.
+
+## Suites après exp-00068
+
+- [À privilégier] Profiler séparément le coût restant de `encode_observation`, du pooling et de la
+  construction des représentations d'actions avant une nouvelle optimisation runtime.
+- [À étudier] Évaluer une réutilisation bornée des représentations d'actions uniquement si elle peut
+  respecter les identifiants et l'état observable ; mesurer d'abord son taux réel de réutilisation.
+- [À supprimer] Toute modification de pooling qui change les trajectoires ou toute cache active en
+  entraînement sans stratégie explicite d'invalidation.
+
 
 ## Décisions issues de la campagne exp-00050 à exp-00055
 

## Provenance

```json
{
  "budget_seconds": 3600,
  "campaign_id": "campaign-20260806-152324-f0a6e2",
  "dataset": null,
  "dataset_records": null,
  "dataset_sha256": null,
  "experiment_family": "performance",
  "experiment_kind": "performance",
  "novelty": "The repository history contains runtime measurements embedded in quality experiments but no dedicated performance-family experiment. This pass isolates an inference-only cache with the same v002 checkpoint and fixed game workload.",
  "parent_commit": "78348de2d4f0649cad19f13a63149d2c0a2a9297",
  "parent_profile": "v002",
  "seed": 104,
  "teacher_profile": null
}
```

## Changements autorisés

```json
[
  "doc/Current state/Neural player.md",
  "doc/Ideas.md",
  "shards_ai/ai/neural_model.py",
  "result.json"
]
```

## Recette d'entraînement

```json
{}
```

## Commandes

```json
[
  "codex exec --sandbox workspace-write --ephemeral -",
  "poetry run pytest -q"
]
```

## Résultats offline

```json
{}
```

## Observations analytiques

```json
{}
```

## Limites de l'analyse

```json
{}
```

## Recommandations

```json
{}
```

## Résultats de screening

```json
{}
```

## Validation

```json
{
  "diff_check": "git diff --check",
  "result": "307 passed",
  "tests": "PYTHONPATH=. poetry run pytest -q"
}
```

## Promotion

```json
{}
```

## Tests fixes

```json
{
  "command": "poetry run pytest -q",
  "duration_seconds": 19.072,
  "exit_code": 0,
  "timed_out": false
}
```

## Décision déterministe

```json
{
  "accepted": true,
  "available": true,
  "max_regression": -0.11756830247055752,
  "threshold": 0.02
}
```

## Performance

```json
{
  "baseline": {
    "elapsed_seconds": 14.719090116966981,
    "neural_decisions": 8770,
    "neural_inference_seconds": 5.777275001601083,
    "repetitions_elapsed_seconds": [
      14.569914002058795,
      14.719090116966981,
      14.78783298007329
    ],
    "throughput_games_per_second": 3.3969491050512715,
    "total_actions": 17587
  },
  "candidate": {
    "elapsed_seconds": 12.988591678004013,
    "neural_decisions": 8770,
    "neural_inference_seconds": 4.964674497954547,
    "repetitions_elapsed_seconds": [
      12.939106739970157,
      13.03024313916103,
      12.988591678004013
    ],
    "throughput_games_per_second": 3.849532053938862,
    "total_actions": 17587
  },
  "elapsed_delta_seconds": -1.730498438962968,
  "gate": "accepted: median elapsed time improved by 11.76%, exceeding the 2% performance threshold",
  "protocol": {
    "aggregation": "median",
    "checkpoint": "configs/neural_profiles/v002.pt",
    "games": 50,
    "max_actions": 10000,
    "max_turns": 200,
    "opponent": "v002 NeuralPlayer",
    "player": "v002 NeuralPlayer",
    "repetitions": 3,
    "seeds": "0..49",
    "torch_threads": 1
  },
  "relative_elapsed_change": -0.11756830247055752,
  "relative_inference_change": -0.1406546344810202,
  "relative_throughput_change": 0.13326461306975526
}
```

## Performance gate

```json
{
  "accepted": true,
  "available": true,
  "max_regression": -0.11756830247055752,
  "threshold": 0.02
}
```

## Suivi performance obligatoire

Non renseigné

## Analyse humaine

```json
{
  "attribution": "The baseline profile put 0.473 seconds in _embedding_lookup and 0.383 seconds in _card_embeddings over five representative games, while NeuralPlayer scoring dominated the runtime. The candidate keeps the same checkpoint, seeds, players, action limit, thread count and action/decision counts, so the elapsed reduction is attributable to avoiding repeated static embedding computation.",
  "change": "Added an inference-only per-scorer cache in shards_ai/ai/neural_model.py. Missing card vectors are computed once and reused; training mode and gradient-enabled execution use the original uncached path.",
  "performance_gate_only": true,
  "quality_evidence_used": false
}
```

## Limites et suite

À compléter par l'agent ou l'analyse humaine.
