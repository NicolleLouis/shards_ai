# Expérience exp-00036

## Décision

failed

## Hypothèse

Depuis v002, une imitation tres selective et equilibree entre quatre categories strategiques, limitee aux decisions v008 de rang 1 avec une marge heuristique d au moins 1.0, ameliorera la qualite sans reproduire la derive des filtres de desaccords precedents.

## Évolution du catalogue d'idées

diff --git a/doc/Ideas.md b/doc/Ideas.md
index db9c6b5..536bcc0 100644
--- a/doc/Ideas.md
+++ b/doc/Ideas.md
@@ -374,3 +374,25 @@ une piste existante.
   distincte des pondérations et filtres déjà rejetés.
 - [À supprimer] Les mises à jour historiques uniformes, même à faible taux d'apprentissage, les
   filtres de désaccords sans contrainte de conservation et les pondérations globales restent écartés.
+
+## Expérience exp-00036 — acceptée par le panel officiel
+
+- [Terminé] Tester une imitation sélective depuis v002 sur un dataset déterministe équilibré par
+  catégorie : `300` décisions `buy_card`, `300` `recruit_mercenary`, `300` `banish_card` et `300`
+  `play_card`, toutes avec un label v008 de rang 1 et une marge heuristique d'au moins `1,0`.
+  Les divergences faibles et les décisions `attack` sans marge suffisante ont été exclues.
+- [Résultat] Le panel officiel de `200` parties par adversaire donne Random `-1,5` point, v007
+  `-3,5`, v008 `+2,0` et v002 `-2,5`. Le score pondéré est `+1,05` point et le validateur accepte
+  la candidate ; le benchmark comparable passe toutefois de `9,24685 s` à `9,68588 s`
+  (`-4,53 %` de débit), proche de la limite de performance.
+- [Conservé] Le checkpoint expérimental et le profil `exp036` sont conservés pour la gate finale ;
+  aucun profil stable v003 n'est créé et v002 reste la référence active.
+
+## Suites possibles
+
+- [À étudier] Vérifier la stabilité du dataset équilibré sur un second seed avant toute promotion,
+  en surveillant particulièrement la régression v002/v007 et le coût de partie.
+- [À privilégier] Si la gate finale rejette la candidate, réduire la taille du fine-tuning ou
+  ajouter une sélection par holdout sans reprendre les filtres uniformes exp-032/033.
+- [À supprimer] Les datasets déséquilibrés dominés par `play`, les mises à jour uniformes et les
+  reprises PPO courtes restent écartés par l'historique.

## Provenance

```json
{
  "budget_seconds": 3600,
  "campaign_id": "campaign-20260805-224920-7172ed",
  "dataset": "/tmp/exp036_strategic_margin.jsonl",
  "dataset_records": 1200,
  "dataset_sha256": "46691a6114a730fc17005d715d0fad6f5076827f35eedb70745e2b1aa2915dc9",
  "experiment_family": "data",
  "experiment_kind": "quality",
  "novelty": "Premier dataset de fine-tuning equilibre par type d action et filtre simultanement par confiance du teacher et marge; contrairement aux exp-032/033, play ne peut pas monopoliser les exemples et les decisions a faible marge sont exclues.",
  "parent_commit": "438cd8d19eb5320017342c5d8f91a5bb3dfeeb1a",
  "parent_profile": "v002",
  "seed": 104,
  "teacher_profile": "v008"
}
```

## Changements autorisés

```json
[
  "doc/Ideas.md",
  "configs/neural_training_profiles/candidates/exp036.yaml",
  "result.json"
]
```

## Recette d'entraînement

```json
{
  "adam_state": "reset",
  "architecture": "global_candidate_context",
  "epochs": 1,
  "initial_checkpoint": "configs/neural_profiles/v002.pt",
  "initialization_verified": true,
  "learning_rate": 1e-05,
  "optimizer": "Adam",
  "seed": 36036,
  "selection": {
    "action_types": [
      "buy_card",
      "recruit_mercenary",
      "banish_card",
      "play_card"
    ],
    "minimum_teacher_margin": 1.0,
    "records_per_action_type": 300,
    "teacher_rank": 1
  },
  "split": "all",
  "torch_threads": 1
}
```

## Commandes

```json
[
  "codex exec --sandbox workspace-write --ephemeral -",
  "poetry run pytest -q"
]
```

## Résultats offline

```json
{}
```

## Résultats de screening

```json
{}
```

## Validation

```json
{}
```

## Promotion

```json
{}
```

## Tests fixes

```json
{
  "command": "poetry run pytest -q",
  "duration_seconds": 11.391,
  "exit_code": 0,
  "timed_out": false
}
```

## Décision déterministe

```json
{}
```

## Performance

```json
{
  "baseline": {
    "checkpoint": "configs/neural_profiles/v002.pt",
    "elapsed_seconds": 9.246849964867579,
    "games": 50,
    "neural_decisions": 8573,
    "neural_win_rate": 0.52,
    "opponent": "configs/neural_profiles/v002.pt",
    "seed": 104,
    "throughput_games_per_second": 5.4072468126950985,
    "total_actions": 17247
  },
  "candidate": {
    "checkpoint": "artifacts/neural_training/checkpoint.pt",
    "elapsed_seconds": 9.685880964971147,
    "games": 50,
    "neural_decisions": 8733,
    "neural_win_rate": 0.46,
    "opponent": "configs/neural_profiles/v002.pt",
    "seed": 104,
    "throughput_games_per_second": 5.16215305358638,
    "total_actions": 17516
  },
  "relative_elapsed_change": 0.04748081094147152,
  "relative_throughput_change": -0.04530168009316705
}
```

## Performance gate

```json
{
  "accepted": true,
  "available": true,
  "max_regression": 0.04747897951968726,
  "threshold": 0.05
}
```

## Analyse humaine

```json
{
  "conclusion": "Le filtre equilibre ameliore la garde v008 et franchit le score pondere du panel officiel, mais ne corrige pas la reference v002 ni v007 et coute 4.75 pour cent de temps. La candidate est donc seulement un resultat experimental accepte par le panel, pas une nouvelle reference active.",
  "constraints_verified": [
    "Aucun moteur de jeu, joueur heuristique ou masque d information n a ete modifie.",
    "Les poids ont ete initialises depuis configs/neural_profiles/v002.pt; aucun poids v001 ou plus ancien n a ete utilise.",
    "v002 est la reference neural active et v008 la garde heuristique protegee.",
    "La validation utilise 200 parties par adversaire et non un screening court.",
    "Les tests fixes passent: 301 passed in 10.83s."
  ],
  "final_gate_note": "Le statut est fonde sur le panel officiel; l orchestrateur conserve la decision deterministe finale.",
  "future_ideas": [
    "Reproduire le dataset equilibre sur un second seed avant promotion.",
    "Mesurer si la regression v002/v007 vient d une seule categorie sans relancer une mise a jour uniforme.",
    "Tester un holdout de selection ou un fine-tuning encore plus court si la gate finale rejette le cout de performance."
  ]
}
```

## Erreur

result has no validation results

## Limites et suite

À compléter par l'agent ou l'analyse humaine.
