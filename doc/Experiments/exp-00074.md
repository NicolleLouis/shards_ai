# Expérience exp-00074

## Décision

rejected

## Hypothèse

Removing only the reference-policy KL penalty from a PPO continuation initialized from active v002 will enable a non-degenerate update and improve playing strength without regressing Random, v007, or v008.

## Évolution du catalogue d'idées

diff --git a/doc/Ideas.md b/doc/Ideas.md
index dc805a7..c3367c7 100644
--- a/doc/Ideas.md
+++ b/doc/Ideas.md
@@ -652,3 +652,31 @@ métriques secondaires et ne peuvent pas compenser un échec de qualité.
   reprise vérifiée après chaque palier et holdout séparé.
 - [À conserver] Exiger Random, v007, v008 et neural v002, ainsi qu'un runtime comparable ; le gain
   contre Random seul ne suffit jamais.
+
+## Expérience exp-00074 — PPO — rejetée
+
+- [Terminé] Tester une continuation PPO depuis `configs/neural_profiles/v002.pt` avec
+  `gamma=0,995` et `gae_lambda=0,95`, en supprimant uniquement la pénalité KL vers v002
+  (`reference_kl_coefficient=0`). La nouveauté est de libérer l'actualisation après le signal
+  PPO non nul mais quasi nul observé en exp-00071, sans changer le moteur, les heuristiques ou le
+  masque d'information.
+- [Résultat] Le checkpoint canonique a été écrit après le premier palier atteint : 16 parties,
+  2 476 transitions, `approx_kl=0,0252`, clipping `0,1022` et perte de politique `0,0183`.
+  L'exécution prévue de 64 parties n'a pas dépassé ce palier dans l'environnement.
+- [Résultat] Le screening batché de 20 parties par adversaire donne Random `-20`, v007 `-5`,
+  v008 `-15` et neural v002 `+20` points de delta. Le gain contre v002 neural est isolé et les
+  trois références obligatoires régressent ; aucune validation longue ni promotion n'est justifiée.
+- [Résultat] Le benchmark comparable v002-v002 passe de `9,3434 s` à `9,4915 s` (`+1,59 %`),
+  avec `6 774` contre `7 085` actions et une inférence de `3,4492 s` à `3,4072 s`.
+- [Supprimé] Ne pas reprendre la suppression KL seule : elle libère bien une dérive de politique,
+  mais cette dérive dégrade Random, v007 et v008.
+
+## Nouvelles idées après exp-00074
+
+- [À privilégier] Instrumenter et comparer les avantages, gradients et décisions modifiées par
+  phase/action sur un holdout séparé avant tout nouveau PPO ; le signal d'actualisation est réel,
+  mais sa direction est manifestement mal alignée avec les trois références.
+- [À étudier] Tester une continuation PPO avec KL non nul mais borné et une sélection par holdout
+  indépendant, uniquement si un budget d'argmax modifiés et une attribution causale sont définis.
+- [À supprimer] Toute suppression globale de KL, toute campagne PPO interrompue avant un budget
+  comparable et tout gain contre le seul adversaire neural v002.

## Provenance

```json
{
  "budget_seconds": 3600,
  "campaign_id": "campaign-20260806-152324-f0a6e2",
  "dataset": null,
  "dataset_records": null,
  "dataset_sha256": null,
  "experiment_family": "ppo",
  "experiment_kind": "quality",
  "novelty": "Isolated reference_kl_coefficient=0 after exp-00071 near-zero PPO updates, with v002 initialization and unchanged engine, heuristics, information mask, and architecture.",
  "parent_commit": "64e61876d3b6dabad48f46b24c34974c34a5216a",
  "parent_profile": "v002",
  "seed": 104,
  "teacher_profile": null
}
```

## Changements autorisés

```json
[
  "doc/Ideas.md",
  "result.json",
  "configs/neural_training_profiles/candidates/exp00074-ppo-v002-no-kl.yaml",
  "doc/Experiments/exp-00074.md"
]
```

## Recette d'entraînement

```json
{
  "actual_games": 16,
  "gae_lambda": 0.95,
  "games_per_update": 16,
  "gamma": 0.995,
  "initial_checkpoint": "configs/neural_profiles/v002.pt",
  "learning_rate": 0.0001,
  "method": "ppo",
  "minibatch_size": 2048,
  "optimization_epochs": 4,
  "profile": "configs/neural_training_profiles/candidates/exp00074-ppo-v002-no-kl.yaml",
  "reference_kl_coefficient": 0.0,
  "reset_optimizer": true,
  "seed": 74074,
  "torch_threads": 1,
  "total_games_requested": 64,
  "transitions": 2476
}
```

## Commandes

```json
[
  "codex exec --sandbox workspace-write --ephemeral -",
  "poetry run pytest -q"
]
```

## Résultats offline

```json
{}
```

## Observations analytiques

```json
{}
```

## Limites de l'analyse

```json
{}
```

## Recommandations

```json
{}
```

## Résultats de screening

```json
{}
```

## Validation

```json
{
  "accepted": false,
  "protocol": "validate_neural_profile_batched.py, 20 games per opponent, batch_games=20, progress-output, same seeds against active v002",
  "reason": "Required Random, v007, and v008 non-regression failed; isolated neural:v002 gain is insufficient.",
  "reference_profile": "v002",
  "results": {
    "neural:v002": {
      "candidate_win_rate": 0.6,
      "delta_win_rate": 0.2,
      "games": 20,
      "reference_win_rate": 0.4
    },
    "random": {
      "candidate_win_rate": 0.75,
      "delta_win_rate": -0.2,
      "games": 20,
      "reference_win_rate": 0.95
    },
    "v007": {
      "candidate_win_rate": 0.35,
      "delta_win_rate": -0.05,
      "games": 20,
      "reference_win_rate": 0.4
    },
    "v008": {
      "candidate_win_rate": 0.1,
      "delta_win_rate": -0.15,
      "games": 20,
      "protected_heuristic_guard": true,
      "reference_win_rate": 0.25
    }
  }
}
```

## Promotion

```json
{}
```

## Tests fixes

```json
{
  "command": "poetry run pytest -q",
  "duration_seconds": 26.636,
  "exit_code": 0,
  "timed_out": false
}
```

## Décision déterministe

```json
{
  "accepted": false,
  "deltas": {
    "neural:v002": 0.2,
    "random": -0.2,
    "v007": -0.05,
    "v008": -0.15
  },
  "mean_delta_win_rate": -0.05,
  "minimum_mean_delta": 0.0,
  "opponent_count": 4,
  "performance_followup_required": false,
  "performance_gate": {
    "accepted": true,
    "available": true,
    "max_regression": 0.01584709141721532,
    "threshold": 0.05
  },
  "reason": "v008_guard_or_mean_gain_failed",
  "v008_floor": 0.0
}
```

## Performance

```json
{
  "baseline": {
    "checkpoint": "configs/neural_profiles/v002.pt",
    "elapsed_seconds": 9.343445379985496,
    "neural_inference_seconds": 3.449164250312606,
    "throughput_games_per_second": 2.1407,
    "total_actions": 6774
  },
  "candidate": {
    "checkpoint": "artifacts/neural_training/checkpoint.pt",
    "elapsed_seconds": 9.491511813073885,
    "neural_inference_seconds": 3.407198643486481,
    "throughput_games_per_second": 2.107,
    "total_actions": 7085
  }
}
```

## Performance gate

```json
{
  "accepted": true,
  "available": true,
  "max_regression": 0.01584709141721532,
  "threshold": 0.05
}
```

## Suivi performance obligatoire

Non renseigné

## Analyse humaine

```json
{
  "future_ideas": [
    "Attribute advantage, gradient, and argmax changes by phase/action on an independent holdout before another PPO run.",
    "Only consider a bounded nonzero KL continuation after fixing an argmax-change budget and independent holdout.",
    "Do not retry global KL removal or a gain against neural v002 alone."
  ],
  "limitations": [
    "Training stopped after 16 of 64 requested games in the environment.",
    "The screening is diagnostic and not a full promotion gate; no long validation was warranted after clear regressions."
  ],
  "result": "The KL removal created a measurable policy update, but its direction reduced strength against all required non-neural references."
}
```

## Erreur

v008_guard_or_mean_gain_failed

## Limites et suite

À compléter par l'agent ou l'analyse humaine.
